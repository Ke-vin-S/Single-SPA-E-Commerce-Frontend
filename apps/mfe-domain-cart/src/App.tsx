import React from 'react';

// The cart domain MFE has no dedicated UI surface; its purpose is to inject the
// reducer and expose the manager + hook. Mount renders nothing.
export const App: React.FC = () => null;
